.app-header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 112px;
  background: rgba(0, 0, 0, 0.9);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  z-index: 999;
  display: flex;
  flex-direction: column;
  justify-content: center; /* Centrat de la fila mestre */
  padding: 0 24px;
  box-shadow: 0 4px 40px rgba(0, 0, 0, 0.6);
}

.header-top-row {
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.header-nav-row {
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.header-master-nav {
  display: flex;
  gap: 32px;
  align-items: center;
}

.header-master-link {
  display: flex;
  align-items: center;
  gap: 8px;
  color: rgba(255, 255, 255, 0.6);
  text-decoration: none;
  font-size: 14px;
  font-weight: 700;
  padding: 6px 12px;
  border-radius: 8px;
  transition: all 0.3s ease;
}

.header-master-link:hover {
  color: #fff;
  background: rgba(255, 255, 255, 0.1);
}

.header-master-link.active {
  color: #00D2FF;
  background: rgba(0, 210, 255, 0.1);
}

.header-main-logo {
  height: 32px;
  width: auto;
  filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.3));
}

.bar-trailing {
  display: flex;
  align-items: center;
  gap: 12px;
}

.bar-icon-btn {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  background: transparent;
  border: none;
  cursor: pointer;
  border-radius: 50%;
  transition: all 0.2s ease;
}

.bar-icon-btn:hover {
  background: rgba(255, 255, 255, 0.1);
}

.bar-badge {
  position: absolute;
  top: 4px;
  right: 4px;
  background: #FF3B30;
  color: #fff;
  font-size: 10px;
  font-weight: 800;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid #000;
}

.bar-avatar-wrapper {
  margin-left: 8px;
}

.bar-avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  overflow: hidden;
  border: 2px solid rgba(255, 255, 255, 0.2);
}

.bar-avatar img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

@media (max-width: 768px) {
  .app-header-master {
    height: auto;
    padding: 0 16px;
  }
  
  .header-master-nav {
    gap: 16px;
    overflow-x: auto;
    padding-bottom: 8px;
    width: 100%;
    justify-content: flex-start;
  }
  
  .header-master-link span {
    display: none;
  }
}
