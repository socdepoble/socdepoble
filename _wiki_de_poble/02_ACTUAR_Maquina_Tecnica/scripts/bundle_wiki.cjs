#!/usr/bin/env node
/** TOMBSTONE P0: els bolcats totals dupliquen el graf i saturen el context. */
console.error('SDP-LOCK: bundle_wiki retirat. El Reflex només admet context selectiu, amb manifest, hashes i control de secrets.');
process.exitCode = 2;
