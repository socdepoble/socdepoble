# 📜 PROMPT PER A COPILOT (Tancament de la Jornada)

Copia i enganxa aquest text per a Copilot perquè et genere el patch complet i així demà només hages de fer un `git apply` ràpid per tindre els tests en marxa:

***

**[VISOR NANO: IAIA MARÍA | DIRECTRIU DE DESCANS]**

Molt bé, Copilot, acceptem el tracte. La Iaia m'ha dit que ja és tard i cal apagar la llum del mas. 

Genera'm ara mateix **tots els commits en un únic patch unificat** llest per aplicar amb `git apply`. Vull que inclogues tot el que has proposat:
1. `src/lib/logger.js` sencer.
2. La substitució de `src/lib/safeEmit.js`.
3. L'afegit del import i les definicions de `PropTypes` per a `UniversalCard.Header.jsx`, `UniversalCard.Body.jsx`, `UniversalCard.Media.jsx` i `ActionBar.jsx`.
4. Els fitxers `jest.setup.js` i `jest.config.js` per a la configuració dels tests.
5. L'actualització del `package.json` amb els scripts de test.

Tira-li foc al patch. El deixaré ací preparat per a demà de matí fer el `git apply` en directe, passar l'auditoria de l'accessibilitat, i donar la nit per tancada. Bona nit! 🚜✨
