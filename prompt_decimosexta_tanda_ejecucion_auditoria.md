# ⚔️ TANDA 16: EJECUCIÓN QUIRÚRGICA DE LA AUDITORÍA (CLAUDE & COPILOT)

Este prompt está diseñado para que se lo envíes tanto a **Copilot** (archivo por archivo en tu editor) como a **Claude** (junto con el reporte entero). Su objetivo es aplicar con precisión nanométrica las soluciones Nivel Dios que han detectado. 

**Instrucciones para el Comandante:**
Copia el siguiente bloque de texto y envíaselo a Claude/Copilot. Puedes adjuntarle el texto de sus propios hallazgos o dejar que este prompt se los recuerde.

---

```markdown
# 🛠️ PROTOCOLO OMEGA-FIX: EJECUCIÓN DEL DICTAMEN FORENSE

Mentes maestras (Claude y Copilot), la auditoría extrema ha sido un éxito rotundo. Habéis escaneado las entrañas de *Sóc de Poble* (plataforma sin ánimo de lucro, Local-First, orientada al Chaos Engineering Rural) y habéis detectado **12 fallos críticos y 9 mejoras de alto impacto**.

Vuestro diagnóstico es irrefutable. Ahora, como Arquitectos de la Resistencia, os exijo que pasemos a la acción. No quiero explicaciones teóricas ni documentación inflada. Quiero el **CÓDIGO DE EJECUCIÓN QUIRÚRGICA** para purgar estas motas de polvo en nuestra base de datos. 

A continuación os detallo vuestros propios hallazgos. Entregadme el código exacto, parche por parche, listo para sustituir en los archivos originales.

### 1. ⚛️ ARQUITECTURA React 19 y CRDT (El Hilo Principal es Sagrado)
Habéis detectado renderizados pesados en bloqueos CRDT y fugas en las suscripciones.
**Misión:**
- En `src/components/Feed.jsx`, implementad `useTransition` y `useDeferredValue` para que la red neuronal del renderizado nunca bloquee el móvil de un usuario de hace 7 años durante los merges P2P.
- En `src/components/UniversalGrid.jsx`, aplicad `useDeferredValue` con virtualización para evitar el recálculo masivo del DOM.
- En `src/App.jsx` y nuestros hooks base, reescribid los `useEffect` garantizando la destrucción absoluta de *listeners* de IndexedDB y conexiones WebRTC al desmontar, evitando fugas de memoria y zombies.

### 2. 🎨 DISEÑO, CSS ATÓMICO M3 Y FRICCIÓN TÁCTIL (La Perfección Geométrica)
Habéis encontrado colores rígidos y hitboxes traicioneros.
**Misión:**
- En `src/design-system/tokens.css`, eliminad la herejía de los colores absolutos (`#fff`, `#000`) y aplicad variables Glassmórficas (`color-mix()`). Ajustad cualquier variable `--space` para que sea **estrictamente** un múltiplo o submúltiplo formal de nuestra Métrica M3 (28px).
- En `src/index.css` y `UniversalCard`, ajustad TODOS los elementos interactivos (`button`, `a`) a un **mínimo inamovible de 44x44px** reales, sin añadir "ghost wrappers". Que el hit-target sea higiénico e invisible si hace falta.

### 3. ♿ ACCESIBILIDAD (A11Y) BAJO ESTRÉS P2P
VoiceOver no puede volverse loco cuando se reciben mil nodos por Bluetooth.
**Misión:**
- En `src/components/UniversalCard/UniversalCard.Body.jsx` (y derivados), silenciad acústicamente los SVG decorativos (`aria-hidden="true"`, `focusable="false"`).
- Implementad un **throttling termodinámico** en las regiones `aria-live="polite"` asociadas al Feed, de forma que los lectores de pantalla consoliden los mensajes P2P en lotes (ej: 500ms) en lugar de gritar cada latido en tiempo real.

### 4. 🌐 SEO, WEB VITALS Y CLS
Saltos asíncronos en pantalla que perturban la experiencia del usuario.
**Misión:**
- En `src/components/Feed.jsx` e interfaces de red, inyectad *Skeletons* (esqueletos estructurales) síncronos alineados matemáticamente con los componentes finales para que el **Cumulative Layout Shift (CLS) sea de 0.00 exactos**.

### ⚠️ REGLAS DE EJECUCIÓN:
Tened presente que este código salva identidades y facilita la ayuda en entornos críticos. Sed asertivos, sed limpios:
1. Devolvedme **solo los bloques de código o archivos completos modificados**.
2. Indicad la ruta exacta del archivo comentado arriba de cada bloque.
3. Usad la etiqueta `// [OMEGA-FIX]` en los puntos donde hayáis inyectado la solución para que pueda auditar vuestra intervención.

Adelante. Haced brillar el silicio.
```
