<USER_REQUEST>
Copilot: ### **Paquet complet ready‑to‑paste — RhizomeManagerV3 i eines**

---

#### **`src/rhizome/RhizomeManagerV3.js`** (Main thread, Two‑Phase Commit + SafeGC)
```js
// RhizomeManagerV3.js
// Main thread orchestrator: buffer, micro-merge, two-phase commit handshake with worker, SafeGC triggers.

import * as Y from 'yjs';

const MICRO_BATCH = 32;
const FLUSH_MS = 2000;
const SNAPSHOT_THRESHOLD = 500;
const STORAGE_HIGH_WATERMARK = 0.80;
const TPC_TIMEOUT_MS = 10_000; // two-phase commit timeout

let buffer = [];
let flushScheduled = false;
let worker = null;
let ydocRef = null;
let pendingTPC = new Map(); // tpcId -> {resolve,reject,timeout}

function genId() { return `${Date.now()}-${Math.random().toString(36).slice(2,9)}`; }

export function initRhizomeV3(ydoc, opts = {}) {
  ydocRef = ydoc;
  const workerPath = opts.workerPath || '/workers/rhizome-worker.js';
  worker = new Worker(workerPath, { type: 'module' });
  worker.onmessage = handleWorkerMessage;
  worker.onerror = (e) => console.error('Rhizome worker error', e);

  try { if (navigator.storage && navigator.storage.persist) navigator.storage.persist(); } catch(e){}

  ydoc.on('update', onYUpdate);

  document.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'hidden') flushNow();
  });
  window.addEventListener('pagehide', () => flushNow());

  // periodic quota check
  setInterval(checkQuotaAndMaybeGC, 30_000);
}

function onYUpdate(update) {
  // update is Uint8Array or ArrayBuffer
  buffer.push(update);
  if (buffer.length >= MICRO_BATCH) flushNow();
  else scheduleFlush();

  if (buffer.length >= SNAPSHOT_THRESHOLD) {
    // request snapshot flow via worker (worker will ask main for state)
    worker.postMessage({ type: 'snapshotRequest' });
  }
}

function scheduleFlush() {
  if (flushScheduled) return;
  flushScheduled = true;
  if (typeof requestIdleCallback === 'function') {
    requestIdleCallback(() => flushNow(), { timeout: FLUSH_MS });
  } else {
    setTimeout(() => flushNow(), FLUSH
<truncated 23055 bytes>
storage formats

User-facing messages (human-friendly):
- "Guardant la faena localment…": shown when saving snapshot
- "El tractor s'ha calat; fem una còpia i tornem a arrencar": shown on emergency GC or storage pressure
- "Còpia de seguretat de la clau creada. Guarda aquest fitxer en un lloc segur.": after export
```

---

### Notes finals i recomanacions d’integració
- **Substituir demo key**: abans de producció, no deixeu cap clau dura en codi. Proveu OPFS + passphrase derivada o un KMS per a la provisió segura de `cryptoKey` al worker (via `postMessage` setKey with raw key bytes transferred).
- **Worker bundling**: si utilitzeu bundler (Vite, Webpack), empaqueteu el worker com a module worker o col·loqueu-lo a `public/workers/`.
- **Telemetry i alerts**: registreu temps de merge/compress/encrypt i percentatge d'ús d'espai; configureu alertes si compress ratio cau o si tpc timeouts augmenten.
- **Tests reals**: executeu les proves en dispositius reals (iPad 2016, Safari iOS) i amb condicions d'emmagatzematge limitat per validar SafeGC.
- **Trellat**: afegiu missatges humans i opcions d'export manual per a usuaris que vulguen garantir la seva recuperació.

---

Si voleu, us lliure ara mateix aquests fitxers en un gist o us preparo un patch `git` (diff) amb tots els fitxers llistats perquè només hàgiu de fer `git apply`. Digueu‑me quin format preferiu i us ho preparo. Llum de gas i a posar la teulada. 🌾🚜🔥
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-10T11:00:13+02:00.

The user's current state is as follows:
Browser State:
  Page BA1F7BAE17E2F3BBF6589C3DE2EFD04B (Sóc de Poble | Skills) - https://socdepoble.org/skills [ACTIVE]
    Viewport: 2360x1289, Page Height: 1289
  Page 7D366A4470002738009FB51E04FBF0AD (Error) - http://localhost:3000/anima
    Viewport: 3367x1289, Page Height: 1289
  Page 7404197A2B0A9DAAD4888A61B5C5E160 (Sóc de Poble | L'Ànima de la IAIA) - https://socdepoble.org/anima
    Viewport: 3367x1289, Page Height: 1289
</ADDITIONAL_METADATA>