import React from 'react';

const GlobalFooter = () => {
  return (
    <footer className="hidden">
      {/* Footer invisible o minimalista, la info està a la sidebar */}
    </footer>
  );
};

export default GlobalFooter;
