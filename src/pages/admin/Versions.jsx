import UniversalPage from '../public/UniversalPage';

const Versions = () => {
    return (
        <UniversalPage 
            standAlone={true}
            slug="versions"
        />
    );
};

export default Versions;
